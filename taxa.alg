algoritmo "porcentagem"
var
   taxa, muamba: real
inicio
      Escreval("Qual o valor da sua muamba?")
      leia(muamba)
      taxa <- muamba * 60 / 100
      Escreva("Voce foi taxada em R$ ", taxa:5:2)
fimalgoritmo
